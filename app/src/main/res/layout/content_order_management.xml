<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_order_management"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.zeitgeist.employee.Activities.OrderManagementActivity"
    tools:showIn="@layout/activity_order_management">

    <Button
        android:id="@+id/order_search_btn"
        android:layout_width="180dp"
        android:layout_height="50dp"
        android:layout_margin="10dp"
        android:layout_alignParentEnd="true"
        android:textAllCaps="false"
        android:text="Search"
        android:textSize="22dp"
        android:textColor="@color/btn_dark_text"
        android:background="@drawable/login_btn_bg"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/order_list_view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/order_search_btn"/>

</RelativeLayout>
